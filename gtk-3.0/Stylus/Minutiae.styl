/* CHANGELOG
 * Written by Josh Grooms on 20151208
 */



/** EVOLUTION TWEAKS **/
// Governs the small box at the top of the Evolution sidebar (for all tabs)
EShellWindow
    EShellSwitcher
        GtkEventBox
            background-image: vgradient(WindowColor, WindowColor);
            &:backdrop
                background-image: vgradient(WindowColorDim, WindowColorDim);

        GtkScrolledWindow
            flat();
            padding: 0px;

    // MessageList ECanvas .cell
    //     debug();
    //     background-image: vgradient(Red, Red)
    //     border-radius: 10px;

EPreferencesWindow GtkNotebook > GtkAlignment > GtkScrolledWindow
    flat();

// Governs the buttons at the top of each list-view-like element (e.g. e-mail)
ECanvas button
    @extends button.flat;
    color: TextColorDark;
    &:backdrop
        color:  TextColorDark;

/* ECALENDAR - Governs the calendar control widget found within the program 'Evolution'. */
ECalendar
    background-image: none;
    background-color: InsetColor;

    button
        @extends button.flat;
        border-radius: 0.125em;
        GtkArrow
            color: TextColor;
        &:hover GtkArrow
            color: TextColorHighlighted;



/** GNOME TERMINAL **/
vte-terminal.terminal-screen
    background-color:   WindowColorDark;
    color:              VibrantBlue;
    border-radius:      0px;

    terminal-screen-container
        border-radius:  0px;


/** NAUTILUS **/
NautilusWindow
    GtkPaned
        -GtkPaned-handle-size:      2;

    .nautilus-canvas-item
        border-radius:              0.5em;
        &:active                    { background-color: Orchid; }
        &:prelight, &:selected
            background-color:       alpha(SelectionColor, 0.75);
            color:                  TextColorSelected;

            &:backdrop
                background-color:   alpha(SelectionColorBackground, 0.75);

        &.nautilus-desktop, &.nautilus-desktop:backdrop
            color:                  White;



/** UBUNTU SOFTWARE CENTER **/
GtkWindow .section-sel-bg button.flat

    &:checked
        background-image: gradient(WindowColor, WindowColor);

        .symbolic-icon, GtkLabel
            color:              SelectionColor;
            font-weight:        bold;

        &:backdrop
            background-image: gradient(WindowColorDim, WindowColorDim);
            .symbolic-icon, GtkLabel
                color:          HighlightColor;
